info:
  name: products
  type: http
  seq: 1

http:
  method: GET
  url: "{{products}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
