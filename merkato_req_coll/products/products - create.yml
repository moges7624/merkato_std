info:
  name: products - create
  type: http
  seq: 3

http:
  method: POST
  url: "{{products}}"
  body:
    type: json
    data: |-
      {
        "name": "laptop",
        "quantity": 200,
        "price_in_dollar": 1099.99
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
