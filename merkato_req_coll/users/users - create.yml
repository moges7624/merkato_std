info:
  name: users - create
  type: http
  seq: 3

http:
  method: POST
  url: "{{users}}"
  body:
    type: json
    data: |-
      {
        "name": "john doe",
        "email": "joddn@mail.com",
        "password": "passpass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
