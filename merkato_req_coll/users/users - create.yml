info:
  name: users - create
  type: http
  seq: 3

http:
  method: POST
  url: "{{users}}"
  body:
    type: json
    data: |-
      {
        "name": "john doe",
        "email": "johsn@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
