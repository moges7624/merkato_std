info:
  name: permissions - create
  type: http
  seq: 2

http:
  method: POST
  url: "{{permissions}}"
  body:
    type: json
    data: |-
      {
        "user_id": 35,
        "permissions": [1,2,4]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
